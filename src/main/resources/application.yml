spring:
  application:
    name: vetapp

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:vetapp}?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Crea tablas automáticamente
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:stabares_26@cue.edu.co}
    password: ${MAIL_PASSWORD:gntfzaqvxevuxolj}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        debug: false

server:
  port: ${PORT:8080}
  error:
    include-message: always

# Configuración personalizada
app:
  mail:
    from: ${MAIL_USERNAME:stabares_26@cue.edu.co}
    enabled: ${MAIL_ENABLED:true}