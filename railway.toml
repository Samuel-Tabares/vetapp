[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Usar endpoint simple que no requiere BD
healthcheckPath = "/health"
# Tiempo de espera generoso (Spring Boot toma tiempo en iniciar)
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5